# ──────────────────────────────────────────────────────────────────────────
# seo-agent/.env.example  →  copy to seo-agent/.env
# .env is git-ignored. POSTGRES_PASSWORD must match the value in shared/.env.
# ──────────────────────────────────────────────────────────────────────────
# Dedicated DB role for this agent (must match SEO_DB_PASSWORD in shared/.env)
SEO_DB_PASSWORD=change_me_seo_password

# Add agent-specific secrets below (API keys, model endpoints, etc.)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
